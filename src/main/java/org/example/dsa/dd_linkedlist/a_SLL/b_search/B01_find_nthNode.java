package org.example.dsa.dd_linkedlist.a_SLL.b_search;

/**
 * ********************************************************************************
 * Get Nth node in a SLL
 * ********************************************************************************
 */

public class B01_find_nthNode {
	/*
	 * traverse the list (maintain a count)
	 * 
	 * at each node do count++
	 * if (count == N)
	 * 	  break
	 * else
	 *    move to next node
	 * 
	 */
}
