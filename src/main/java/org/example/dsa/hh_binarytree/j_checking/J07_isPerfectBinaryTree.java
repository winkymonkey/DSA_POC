package org.example.dsa.hh_binarytree.j_checking;

/**
 * ***************************************************************************************
 * Check if a given Binary Tree is a Perfect Binary Tree (all internal nodes have two children and all leaves are at same level)
 * ***************************************************************************************
 */

public class J07_isPerfectBinaryTree {
	/*
	 * ------------------
	 * int storedLevel = 0;
	 * int level = 0;
	 * 
	 * Start traversing from root & recursively search leaf nodes in left and right subtree.
	 * Pass a variable 'level' in each recursion call.
	 * Inside the method, do 'level++' and check if current node has both left & right child.
	 * If yes, then keep going, otherwise break and return.
	 * If a leaf node is found, and (level!=storedLevel) then break and return.
	 * Otherwise keep searching.
	 * ------------------
	 */
}
